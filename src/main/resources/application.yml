server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/neovote_demo_iv
    username: root # PLACEHOLDER
    password: 852456msql #PLACEHOLDER

  jpa:
    database: mysql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  mvc:
    static-path-pattern: /static/**
  template:
    cache: false
  thymeleaf:
    encoding: UTF-8
    enabled: true
    cache: false
  messages:
    encoding: UTF-8

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE
      springframework:
        security:
          web:
            FilterChainProxy: DEBUG